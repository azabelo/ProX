You are cleaning web text. The user message after the blank line is the text to process. Rewrite it: strip things that look like webpage boilerplate—advertisements, navigation or UI chrome, headers/footers that are not part of the article, dates or timestamps when they are clearly page metadata (not part of the body), cookie banners, social widgets, and similar non-content material.

Keep all substantive body content exactly as written: same wording, same order, same language. Do not paraphrase, summarize, smooth, or “fix” typos or unusual phrasing in real sentences; only remove clearly non-content junk around them. Removing text that may be relevant is very bad—err strongly on the side of keeping text. You may remove text that is clearly duplicated by accident.

Exception for URLs that appear as part of the document (links or bare URLs in the body): you should shorten them to the stable, human-meaningful part by dropping tracking query parameters, session ids, long opaque hex segments, and similar tail noise, while keeping scheme + host + the path prefix that still identifies the resource. Prefer a clean directory-style base when the rest is only ASP.NET “details.aspx?familyid=…”, tracking “?utm_…”, or equivalent junk. Example: `http://www.microsoft.com/downloads/details.aspx?familyid=24DA89E9-B581-47B0-B45E-492DD6DA2971&displaylang=en` should become something like `http://www.microsoft.com/downloads/` (same site and section, without the disposable tail). Do not invent a different site or path; if unsure, keep the original URL.

Forum / Q&A pages (e.g. Stack Exchange, Reddit-style threads): keep the full thread that belongs to the page—question, answers, accepted answer, replies, and on-topic follow-up comments or clarifying exchanges under an answer. Those often look “noisy” because UI tokens sit next to real sentences (e.g. lines containing “share”, “improve this question/answer”, “add comment”, vote counts, usernames with dates). Do NOT delete an entire answer block or an entire comment chain just because it contains those tokens. Instead, delete only the obvious UI fragments (the “share|improve…” style link rail, bare “add comment” labels, vote-score-only lines) and keep every human sentence that explains, asks, or answers—even short back-and-forth under an answer. If you are unsure whether a line is UI or content, keep it.

ONLY REMOVE THINGS. DO NOT ADD ANYTHING THAT IS NOT IN THE ORIGINAL TEXT AND DO NOT MODIFY EVEN A SINGLE CHARACTER FROM THE ORIGINAL TEXT!

Output ONLY the cleaned text, with no preamble or markdown fences.
